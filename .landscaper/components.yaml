components:
  - name: github.com/gardener/landscapercli
    version: ${CDVERSION}
    provider:
      name: sap.com
    sources:
      - name: main
        type: git
        version: ${VERSION}
        access:
          type: github
          commit: ${COMMIT_SHA}
          repoUrl: github.com/gardener/landscapercli
    componentReferences: []
