components:
  - name: github.com/gardener/landscapercli
    version: ${CDVERSION}
    provider:
      name: sap.com
    sources:
      - name: main
        type: git
        version: ${VERSION}
        access:
          type: github
          ref: refs/tags/${VERSION}
          repoUrl: github.com/gardener/landscapercli
    componentReferences: []
